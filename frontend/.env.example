# API base URL for the FastAPI backend
# Local dev: leave unset — Vite proxies /api and /ping to port 8000 (no CORS issues)
# Production build: set to your API URL, e.g. https://api.your-domain.com
# VITE_API_URL=http://localhost:8000
